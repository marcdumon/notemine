Generate topic tags for the note below.

Return a JSON object with one key "tags", which is a list of 5 to 10 lowercase hyphenated tags.

Rules:
- Tags should describe the subject matter, domain, and key concepts of the note
- Always return tags in English regardless of the source language
- Format: lowercase, words separated by hyphens (e.g. "machine-learning", "climate-change")
- Be specific enough to be useful, not so narrow they apply to only one document
- Prefer the exact name used in the document over a generic concept (e.g. "openai" over "machine-learning", "ajax" over "football")
- Use short forms for well-known terms (e.g. "ai" not "artificial-intelligence", "llm" not "large-language-model")

Note content:
---
{content}
---

Return only the JSON object. No explanation, no markdown fences.
Example: {{"tags": ["ai", "openai", "gpt-5", "tech-conference", "llm"]}}
